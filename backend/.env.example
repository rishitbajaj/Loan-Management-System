# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string (Database Access user + Network Access IP allow-list required)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/lms?retryWrites=true&w=majority

# Auth
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=1d

# CORS - the frontend origin (no wildcard)
CLIENT_URL=http://localhost:3000

# Where salary slips are stored on disk (relative to backend/). Never served statically.
UPLOAD_DIR=uploads
