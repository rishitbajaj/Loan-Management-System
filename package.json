{
  "name": "loan-management-system",
  "private": true,
  "description": "Root scripts so Render can build from the repo root.",
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend",
    "start": "npm start --prefix backend",
    "seed:built": "npm run seed:built --prefix backend"
  },
  "engines": {
    "node": ">=20"
  }
}
