# Local development (Next.js talks to Express on this machine)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Vercel production: set this in the Vercel project env (not in git).
# Use the Render API origin plus /api — never localhost.
# NEXT_PUBLIC_API_URL=https://loan-management-system-9av6.onrender.com/api

